on: pull_request

jobs:
  lint-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: wrboyce/python-lint-action@master
      - name: Build
        run: make html
